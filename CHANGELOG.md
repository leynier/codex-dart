## 0.1.0

- Initial release.
- Added Dart SDK wrapper for `codex exec --experimental-json`.
- Added `Codex`, `Thread`, `run`, `runStreamed`, and thread resume support.
- Added support for options, structured output schema, images, config overrides, and cancellation.
- Added unit tests and optional integration tests.
